===============================================================================

	音色転送ユーティリティ VSET Document
						by M.Kajihara(KAJA)

===============================================================================
	VED.EXEで作成された音色データを、PMD内部の音色データエリアに
	転送するTOOLです。

	使用方法は単純で、

>VSET filename.FF	(拡張子は省略不可)

	で転送出来ます。

	PMD内部の音色データエリアは、以下の時に利用されます。

1)	音色データが添付されていない曲データを演奏する場合。
2)	MC(H).EXEによるMMLコンパイル時に音色データを曲データ中に添付する際、
	該当する音色がMML中に定義されていなく、且つコマンドラインに指定された
	音色ファイル(指定されなかった場合を含む)にも存在しなかった場合。

	(詳しくは MC.DOC/MCH.DOC を参照して下さい)

===============================================================================
			  ---- End of VSET.DOC ----
===============================================================================
